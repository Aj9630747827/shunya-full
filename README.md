
# Shunya Landing Page (Cow Craft Naturals + Flexi Glam)

Frontend: React + Vite + Tailwind
Backend: Express + SQLite

Quick Start:
1. cd frontend && npm install && npm run dev
2. cd backend && npm install && npm start
